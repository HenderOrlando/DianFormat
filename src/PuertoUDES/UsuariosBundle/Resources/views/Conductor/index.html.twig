{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Conductor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Canonical</th>
                <th>Descripcion</th>
                <th>Fechacreado</th>
                <th>Direccion</th>
                <th>Telefono</th>
                <th>Docid</th>
                <th>Numlicencia</th>
                <th>Numlibretatripulante</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Conductor__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.canonical }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{% if entity.fechaCreado %}{{ entity.fechaCreado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.direccion }}</td>
                <td>{{ entity.telefono }}</td>
                <td>{{ entity.docId }}</td>
                <td>{{ entity.numLicencia }}</td>
                <td>{{ entity.numLibretaTripulante }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Conductor__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Conductor__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('Conductor__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
