{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UnidadCarga list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marca</th>
                <th>Aniofabrica</th>
                <th>Placa</th>
                <th>Fechacreado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('unidadCarga__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.marca }}</td>
                <td>{% if entity.anioFabrica %}{{ entity.anioFabrica|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.placa }}</td>
                <td>{% if entity.fechaCreado %}{{ entity.fechaCreado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('unidadCarga__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('unidadCarga__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('unidadCarga__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
