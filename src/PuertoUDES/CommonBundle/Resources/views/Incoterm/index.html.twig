{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Incoterm list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Canonical</th>
                <th>Descripcion</th>
                <th>Fechacreado</th>
                <th>Sigla</th>
                <th>Categoria</th>
                <th>Anio</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('incoterm__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.canonical }}</td>
                <td>{{ entity.descripcion }}</td>
                <td>{% if entity.fechaCreado %}{{ entity.fechaCreado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.sigla }}</td>
                <td>{{ entity.categoria }}</td>
                <td>{{ entity.anio }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('incoterm__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('incoterm__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('incoterm__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
