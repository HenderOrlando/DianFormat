{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ContenedorMercanciaFormato</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pesobruto</th>
                <td>{{ entity.pesoBruto }}</td>
            </tr>
            <tr>
                <th>Pesoneto</th>
                <td>{{ entity.pesoNeto }}</td>
            </tr>
            <tr>
                <th>Volumen</th>
                <td>{{ entity.volumen }}</td>
            </tr>
            <tr>
                <th>Fechacreado</th>
                <td>{{ entity.fechaCreado|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Numbultos</th>
                <td>{{ entity.numBultos }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('contenedorMercanciaFormato_') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('contenedorMercanciaFormato__edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
