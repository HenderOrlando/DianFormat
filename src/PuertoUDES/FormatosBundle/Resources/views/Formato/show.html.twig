{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Formato</h1>

    {% include 'PuertoUDESFormatosBundle:Formato:_show.html.twig' %}

        <ul class="record_actions">
    <li>
        <a href="{{ path('formato_') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('formato__edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
</ul>
{% endblock %}
