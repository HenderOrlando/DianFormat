{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Condicion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Condicion</th>
                <th>Fechacreado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('condicion_formato__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.condicion }}</td>
                <td>{% if entity.fechaCreado %}{{ entity.fechaCreado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('condicion_formato__show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('condicion_formato__edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('condicion_formato__new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
